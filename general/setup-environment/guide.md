# Environment Setup

To get started, you need to install a few tools.

## Prerequisites
1. **Node.js**: Install the latest LTS version.
2. **VS Code**: Recommended editor.
3. **Git**: For version control.

## Installation
Run the following command to verify your installation:
```bash
node -v
npm -v
git --version
```

Once verified, you are ready to proceed!
